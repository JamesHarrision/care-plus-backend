# Notes

Từ nay chỉ cần prompt AI để generate tài liệu test. Tôi cài Swagger rồi, bro chỉ cần prompt để AI generate doc là xong, không cần xuất PDF!

Prompt mẫu:

```
Generate / Update the Swagger documentation for all API endpoints. Each route is expected to have one block comment representing the API documentation to suit new changes. It should cover all possible cases, including success and error responses. The documentation should be comprehensive and follow the OpenAPI specification. Please ensure that all endpoints are properly documented with clear descriptions, parameters, request bodies, and response schemas. Ensure the returned content (i.e. "message": "Đăng nhập thành công") is in Vietnamese
```

ROUTE /api-docs
